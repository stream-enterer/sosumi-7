# Phase 3 — Widget Signal Model + emFpPlugin API — Exit

**Date:** 2026-04-20
**Branch at capture:** port-rewrite/phase-3-continue @ `35b6303f`

## C1 gate tails

### `cargo fmt --check`
```
(exit 0 — no output)
```

### `cargo clippy --all-targets --all-features -- -D warnings`
```
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.15s
```

### `cargo-nextest ntr`
```
     Summary [  15.125s] 2476 tests run: 2476 passed, 9 skipped
```

### `cargo test --test golden -- --test-threads=1`
```
test result: FAILED. 237 passed; 6 failed; 0 ignored; 0 measured; 0 filtered out; finished in 20.91s

failures:
    composition::composition_tktest_1x
    composition::composition_tktest_2x
    notice::notice_window_resize
    test_panel::testpanel_expanded
    test_panel::testpanel_root
    widget::widget_file_selection_box
```
(6 failures are the same 6 pre-existing failures from Phase 2 baseline — no regression.)

## Metrics at exit

| Metric | Value |
|---|---|
| nextest | 2476 passed / 0 failed / 9 skipped |
| goldens | 237 passed / 6 failed |
| rc_refcell_total | 256 |
| diverged_total | 173 |
| rust_only_total | 18 |

Commands used:
- `rc_refcell_total`: `rg -c 'Rc<RefCell<' crates/ | awk -F: '{sum += $2} END {print sum}'` → 256
- `diverged_total`: `rg 'DIVERGED:' crates/ | wc -l` → 173
- `rust_only_total`: `rg -c 'RUST_ONLY:' crates/ | awk -F: '{sum += $2} END {print sum}'` → 18

## Delta

| Metric | Baseline | Exit | Δ |
|---|---|---|---|
| nextest passed | 2458 | 2476 | **+18** |
| nextest failed | 0 | 0 | 0 |
| nextest skipped | 9 | 9 | 0 |
| goldens passed | 237 | 237 | 0 |
| goldens failed | 6 | 6 | 0 |
| rc_refcell_total | 262 | 256 | **−6** |
| diverged_total | 176 | 173 | **−3** |
| rust_only_total | 17 | 18 | **+1** |

Gate rules:
- nextest delta ≥ 0: PASS (+18)
- goldens passed delta ≥ 0: PASS (0)
- goldens failed delta ≤ 0: PASS (0)

## Invariants

Phase-3 invariants verified at Task 7 (`35b6303f`). Repeat summary:

| Invariant | Result | Notes |
|---|---|---|
| I3a (widget signal fields) | PASS | 8/8 widget files carry `*_signal: SignalId` field(s). emCoreConfigPanel excluded — spec error (C++ exposes no GetXxxSignal methods; confirmed by grep). |
| I3b (widget callbacks use alias) | PASS | 6 documented exclusions; 0 bare `Box<dyn FnMut(` on widget-path callbacks. All exclusions documented with DIVERGED: or rationale comment. See Task 7 ledger I3b exclusion inventory. |
| I3c (clipboard on emGUIFramework) | PASS | `rg clipboard crates/emcore/src/emContext.rs` → 0 matches; clipboard field on `emGUIFramework::App`. |
| I3d (emFpPlugin API takes ConstructCtx) | PASS | `CreateFilePanel`, `CreateFilePanelWithStat`, `TryCreateFilePanel`, `TryAcquireModel`, `TryAcquireModelFromPlugin` all take `&mut dyn ConstructCtx`. |
| I3e (InputDispatchEngine framework-owned top-priority) | PASS | `struct InputDispatchEngine` at `emInputDispatchEngine.rs:16`; `impl emEngine` at :18; registered at `emGUIFramework.rs:150` with `Priority::VeryHigh`. |

Phase-specific metric targets from plan header: none specified beyond gate rules.

## JSON entries closed

- E025: resolved-phase-3
  - resolution_commit: 33d25c72 (B3.4d keystone — widget signal allocation + fires + DIVERGED-block deletions for GetCheckSignal/CheckChanged/Clicked on emCheckButton + emCheckBox)
  - evidence: I3a PASS for all 8 widget files; Task 7 ledger entry cites grep results
- E024: NOT closed in Phase 3 (deferred)
  - phase_3_progress: 44e4aa9b — overwrite-dialog polling methods deleted, Cycle method added observing fsb.file_trigger_signal + overwrite_dialog.finish_signal; genuine closure requires emDialog → emEngine port (documented DIVERGED block at emFileDialog.rs Cycle method in 8a9154f4)
  - status remains: open
