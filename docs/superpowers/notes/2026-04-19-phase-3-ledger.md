# Phase 3 — Widget Signal Model + emFpPlugin API — Ledger

**Started:** 2026-04-20
**Branch:** port-rewrite/phase-3
**Baseline:** see 2026-04-19-phase-3-baseline.md
**Spec sections:** §2 P1/P3, §3.1, §3.4 (clipboard), §3.5, §6 D6.1–D6.5, §4 D4.9, §4 D4.10
**JSON entries to close:** E024, E025

## Bootstrap decisions

- **B11a (stage-only scan):** Phase 3 plan has NO stage-only tasks — every Task 1–7 has its own commit at step end. Pre-commit hook left in place initially. **Revisited 2026-04-20 mid-phase:** Task 3 dispatched BLOCKED on a Phase-2-style coupling issue — widget `PanelBehavior::Input` signatures don't carry `SchedCtx`, and the Phase-1.76 invariant in `emPanelTree::dispatch_input` forbids threading one through. Per handoff pre-authorization, bundled Task 3 + Task 4 into one unit. **Re-revisited same day:** bundled dispatch returned BLOCKED again — subagent correctly assessed the full bundle as too large for single-session gate-green verification given the C++ signal-ordering fidelity requirement across 11 widgets + 226 `.Input(` call sites + PanelCtx extension + ~60 install-site migrations. Split into 4+ gate-green sub-phases: **B3.1** PanelCtx extension (`framework_actions` + `root_context` fields, `as_sched_ctx()`); **B3.2** widget `Input` signature extension to take `&mut PanelCtx`; **B3.3** WidgetCallback alias + callback sig migration; **B3.4** per-widget-group signal allocation + DIVERGED deletions + tests. **B3.4 further split 2026-04-20** — subagent surfaced that 476 widget `::new` call sites + setter ripple + 11-widget C++ fidelity reads + hook re-enable + 11 un-ignores + 11 new tests cannot land as a single keystone with rigor: **B3.4a** `WidgetCallbackRef<T>` alias + migrate lifetime-parametric callbacks; **B3.4b** widget `new(&mut C: ConstructCtx)` + allocate `*_signal: SignalId` fields (no fires yet); **B3.4c** fire signals at Input paths + un-ignore Input-path tests + add regression tests; **B3.4d keystone** thread `&mut SchedCtx` through remaining non-Input setters + un-ignore remaining tests + delete obsolete DIVERGED blocks + re-enable hook. Pre-commit hook remains disabled until end of B3.4d keystone (avoids re-enable/disable thrash; each sub-phase is internally gate-green).
- **Phase-1.5 precondition deviation:** `_pending_inputs` field exists with underscore prefix (unused marker). Phase 3 Task 1 Step 5 wires it and drops the underscore. User approved proceeding (Option A) on 2026-04-20.

## Task log

- **Task 1 (InputDispatchEngine):** commit c7ca9971. Framework-owned engine at `Priority::VeryHigh` drains `pending_inputs` each slice; three winit callback sites migrated from immediate `win.dispatch_input(...)` to enqueue-and-wake. `EngineCtx` gained `pending_inputs` + `input_state` (latter needed by `emWindow::dispatch_input`); `DoTimeSlice` signature + ~20 call sites updated. Gate green. Baseline deltas: nextest +1 (new test → 2459 passed, 9 skipped), goldens 237/6 preserved, rc_refcell_total unchanged, rust_only_total +1 (emInputDispatchEngine.rust_only).
- **Task 2 (clipboard relocation):** commit 8fb275f4. `emContext::clipboard` + `set_clipboard` + `LookupClipboard` deleted; new `RefCell<Option<Box<dyn emClipboard>>>` field on `emGUIFramework::App` chartered per §3.6(a). `EngineCtx` / `SchedCtx` gain `framework_clipboard` field + `clipboard_mut()` accessor; `PanelCtx` gains optional `framework_clipboard` + `with_scheduler_and_clipboard` constructor. `DoTimeSlice` grows 7th parameter (`#[allow(clippy::too_many_arguments)]` per whitelist). `emClipboard::LookupInherited` free function deleted; `emPrivateClipboard::Install` retargeted to framework slot via DIVERGED comment. Behavioral clipboard tests rewritten to install into a standalone slot. Gate green. rc_refcell_total 262 → 256 (delta −6; better than plan target of −1). Invariant I3c satisfied (rg clipboard crates/emcore/src/emContext.rs → 0 matches).
  - Followup commit e167d895: (1) unify `emSubViewPanel` clipboard-None handling — replace the two `Input`-path `panic!` sites with the same `fallback_cb` RefCell pattern `notice`/`HandleNotice` already uses, lifted to a single shared local for both scoped `SchedCtx` builds. (2) `PanelCtx::with_scheduler_and_clipboard` collapsed into builder chain `with_scheduler(...).with_clipboard(fc)` per CLAUDE.md Code Rules; three callers updated (`emPanelCycleEngine`, `tests/support/mod.rs`, `tests/support/pipeline.rs`). (3) `emPrivateClipboard::Install` signature tightened from `&RefCell<Option<Box<dyn emClipboard>>>` to `&mut Option<Box<dyn emClipboard>>`; four behavioral test call sites updated to `Install(&mut slot.borrow_mut())`; DIVERGED comment updated. Gate green — nextest 2459/0/9, goldens 237/6 preserved, clippy clean, rc_refcell_total unchanged at 256.
- **Task B3.2 (widget Input signatures take &mut PanelCtx):** commit 9cc6cacf. All 11 concrete widget `Input` free functions (emButton, emCheckBox, emCheckButton, emColorField, emDialog, emListBox, emRadioBox, emRadioButton, emScalarField, emSplitter, emTextField) extended with a 5th `ctx: &mut PanelCtx<'_>` parameter, matching emSubViewPanel's pre-existing convention. `PanelBehavior::Input` trait method already took `&mut PanelCtx` from prior work (no trait change). `emColorFieldFieldPanel` trait shims (ScalarFieldPanel / TextFieldPanel / CheckBoxPanel / ListBoxPanel / ButtonPanel) thread ctx through to underlying widgets. 226 `.Input(` call sites migrated: widget internal unit tests (added `test_tree()` helper → `PanelCtx::new` with throwaway tree), control-panel forwarding in `emFileManControlPanel` / `emAutoplayControlPanel` / `emMainControlPanel` / `emMainPanel` (thread existing `_ctx`), pipeline/golden test trait shims (thread `_ctx` from trait method), `widget_interaction.rs` golden tests (added `test_tree()` helper + per-test ctx binding). `emView::RecurseInput` unchanged — `emPanelTree::dispatch_input` still constructs a scheduler-less ctx via `PanelCtx::new` (test-harness-only path); production input flows through `emWindow::dispatch_input` with a full-reach `PanelCtx::with_sched_reach` ctx. **Phase-1.76 invariant superseded:** the INVARIANT comment block at `emPanelTree.rs:1641-1651` replaced with a B3.2 note; dispatch_input no longer warns against scheduler-requiring Input overrides — B3.3 migrates callback invocations, B3.4 allocates signals. Widget Input bodies unchanged apart from the param addition (callbacks still invoked as `cb(args)` against old `Box<dyn FnMut(T)>` field type — B3.3 territory). Gate green — cargo fmt clean, clippy `-D warnings` clean, nextest 2459/0/9 (no change), goldens 237/6 (no regression), rc_refcell_total 256 unchanged. No new unsafe, no new `#[allow]`, no new `Rc<RefCell<>>`. Pure input-path plumbing.

- **Task B3.3 (WidgetCallback alias + callback sigs take SchedCtx):** commit 9abcd026. Introduced `pub type WidgetCallback<Args> = Box<dyn for<'a,'b> FnMut(Args, &'b mut SchedCtx<'a>)>` in `emEngineCtx.rs`. Migrated widget callback fields: emCheckButton/emCheckBox.on_check → WidgetCallback<bool>; emButton.on_click/on_eoi → WidgetCallback<()>, on_press_state → WidgetCallback<bool>; emRadioButton/RadioGroup.on_select → WidgetCallback<Option<usize>>; emScalarField.on_value → WidgetCallback<f64>; emSplitter.on_position → WidgetCallback<f64>; emColorField.on_color → WidgetCallback<emColor>; emTextField.on_selection (usize,usize), on_selection_signal (), on_can_undo_redo (bool,bool). Input-path invocations (Button/CheckButton/CheckBox/RadioButton Input bodies + Click/toggle helpers) synthesize `SchedCtx` via `ctx.as_sched_ctx()` and drop the `_ctx` underscore. Public setters invoked from Input paths (emCheckButton::SetChecked, emCheckBox::SetChecked, emButton::Click, RadioGroup::SetCheckedCtx/SetCheckIndexCtx) take `&mut PanelCtx<'_>`; call-site ripple threaded through `emFileManControlPanel::sync_from_config`, `emStocksControlPanel::UpdateControls`, `emStocksItemPanel::UpdateControls`. **Deferred callback fires (DIVERGED-B3.3):** setters called from non-ctx contexts (emSplitter::SetPos, emScalarField::fire_change, emColorField state-sync, RadioGroup::SetChecked called from Drop/config-sync, RadioGroup::SetCheckIndex, RadioGroup::RemoveByIndex/RemoveAll/deregister, emTextField::fire_selection_change/fire_can_undo_redo) update state but skip callback invocation with a `DIVERGED-B3.3:` marker — B3.4 will restore via async signal dispatch. **Narrow WidgetCallback exceptions:** callbacks with lifetime-parametric payloads (`&str`, `&DialogResult`, `&[usize]`) remain plain `Box<dyn FnMut>` with marker comments (emTextField.on_text/on_validate, emDialog.on_finish/on_check_finish, emListBox.SelectionCb/TriggerCb, emFileSelectionBox.SelectionChangedCb/FileTriggerCb). **Non-widget callbacks annotated:** emPainter.op_log_fn, emMiniIpc.MessageCallback, emPriSchedAgent.callback + add_agent, test_harness RecordingBehavior/MutatingBehavior. `PipelineTestHarness::with_panel_ctx_sched` helper added for tests that exercise setter callbacks through a sched-reach ctx. `widget_interaction.rs::dispatch_event` switched to `PanelCtx::with_sched_reach` (previously `PanelCtx::new`). 11 tests marked `#[ignore = "B3.3: callback requires scheduler reach; B3.4 restores dispatch"]` (callback_receives_state, radio_group_callback, remove_by_index_fires_callback, remove_all_clears_everything, callback_on_change, set_value_fires_callback, can_undo_redo_signal_fires, selection_signal_fires, bp13_programmatic_set_check_index_fires_callback, splitter_on_position_callback_fires_during_drag, colorfield_cycle_fires_on_color_callback). Gate green — cargo fmt clean, clippy `-D warnings` clean, nextest 2448/0/20 (11 new #[ignore] for B3.4), goldens 237/6 preserved, rc_refcell_total 256 (unchanged; +1 grep-match from a new comment mention). No new unsafe, no new `#[allow]`, no new `Rc<RefCell<>>`, no signal allocation, no DIVERGED block deletions.

- **Task B3.4a (WidgetCallbackRef<T> alias + lifetime-parametric callback migration):** commit 9778a6b3. Introduced `pub type WidgetCallbackRef<T> = Box<dyn for<'a,'b,'c> FnMut(&'c T, &'b mut SchedCtx<'a>)>` in `emEngineCtx.rs` alongside B3.3's `WidgetCallback<Args>`; three-lifetime HRTB (`'c` payload, `'b` SchedCtx borrow, `'a` scheduler) expresses borrowed-payload callbacks. `T` accepts unsized types (`str`, `[usize]`) without explicit `?Sized` — Rust accepts this for alias-type parameters used only inside `&T`. New sanity test `widget_callback_ref_type_check` constructs `WidgetCallbackRef<str>` and `WidgetCallbackRef<[usize]>`. Migrated type aliases flagged by B3.3 reviewer IMP-1: emTextField.TextChangeCb → `WidgetCallbackRef<str>`; emDialog.DialogFinishCb → `WidgetCallbackRef<DialogResult>`; emListBox.SelectionCb → `WidgetCallbackRef<[usize]>`; emListBox.TriggerCb → `WidgetCallback<usize>` (by-value, flagged as not lifetime-parametric); emFileSelectionBox.SelectionChangedCb → `WidgetCallback<()>`; emFileSelectionBox.FileTriggerCb → `WidgetCallbackRef<str>`. **Return-value callbacks refreshed, not migrated:** emTextField.ValidateCb and emDialog.DialogCheckFinishCb return `bool` (veto semantics) — structurally incompatible with both aliases (both return `()`); DIVERGED-B3.3 markers refreshed to cite return value rather than lifetime as the divergence reason. **Install-site closure updates:** widget test install sites and emFileSelectionBox nested-widget install closures (dir/name text field on_text; files/filter listbox on_selection + on_trigger) gain `_sched: &mut SchedCtx<'_>` second parameter; closure bodies unchanged. **Fire sites with sched reach:** emFileSelectionBox::Cycle fires on_selection / on_trigger via `ectx.as_sched_ctx()` (EngineCtx available). **Fire sites without sched reach (deferred):** emTextField::fire_change (called from SetText + many edit paths), emDialog::Finish (public API), emListBox::fire_selection / TriggerItem (called from Input + public-setter paths) defer firing with the B3.3 `let _ = &self.on_foo;` pattern and a fresh DIVERGED-B3.4b marker pointing at B3.4b/c signal dispatch restoration. 5 unit tests + 6 pipeline tests marked `#[ignore = "B3.4b: ... deferred dispatch; B3.4c restores via signal"]`: callback_fires_on_change, dialog_finish_fires_callback, trigger_callback, selection_callback, trigger_item_fires_callback + the 6 listbox_*_triggers / no_trigger pipeline tests. Gate green — cargo fmt clean, clippy `-D warnings` clean, nextest 2438/0/31 (B3.3 was 2448/0/20; net −10 passing / +11 skipped / +1 new type-check test), goldens 237/6 preserved. No new unsafe, no new `#[allow]`, no new `Rc<RefCell<>>`, no signal allocation, no DIVERGED block deletions. No capture-shape changes in closures. Pre-commit hook remains disabled until B3.4d.

- **Task B3.4b (widget new<C: ConstructCtx> + allocate *_signal: SignalId fields):** commit 824623b3. Added `*_signal: SignalId` field(s) to each widget matching the C++ `Get*Signal` enumeration: emCheckButton.check_signal; emButton.click_signal + press_state_signal; RadioGroup (emRadioButton::Mechanism) check_signal; emTextField.text_signal + selection_signal + can_undo_redo_signal; emColorField.color_signal; emScalarField.value_signal; emFileSelectionBox.selection_signal + file_trigger_signal; emListBox.selection_signal + item_trigger_signal; emSplitter.pos_signal; emDialog.finish_signal. emCheckBox intentionally has no own signal (C++ inherits from emCheckButton; Rust widget has no inheritance but matches C++ by not exposing a CheckBox-specific signal — B3.4c fires via emCheckButton-parity path). Each listed `new(...)` becomes `new<C: ConstructCtx>(ctx: &mut C, ...)` allocating its signal(s) via `ctx.create_signal()`. Signals are allocated but not fired — B3.4c fires them from Input paths and restores callback dispatch; B3.4d cleans up DIVERGED blocks and re-enables the pre-commit hook. **PanelCtx: ConstructCtx** impl added (requires `scheduler` Some; panics otherwise) so widget construction inside behavior `AutoExpand`/`LayoutChildren` paths can use the ambient ctx directly. **TestSched::cc()** helper added to `tests/golden/common.rs` returning `InitCtx<'_>`; golden tests use it instead of synthesizing a SchedCtx. **Widget unit tests** gain a `TestInit` struct (scheduler + framework_actions + root_context) yielding an `InitCtx` via `.ctx()` — all widget unit tests and internal helper tests migrated. **~470 call sites** rippled across emcore / emfileman / emstocks / emmain / tests: make_factor_field + ScalarFieldPanel::new + TextFieldPanel::new (emColorField expansion), FileFieldPanel::new (emStocksControlPanel), ItemWidgets::new / emStocksItemPanel::AutoExpand, ControlWidgets::new / emStocksControlPanel::AutoExpand, emFileDialog::new + CheckFinish, emDialog::ShowMessage, emFileManControlPanel::new, emStocksListBox::{DeleteStocks,CutStocks,PasteStocks,SetInterest,Cycle,attach_list_box} (dialog-creation paths thread ctx), plus the sealed emStocksFilePanel::Input ctrl-shortcut calls (pass `ctx: &mut PanelCtx` directly via the new PanelCtx: ConstructCtx impl). RadioLinearGroup / RadioRasterGroup constructors take ctx for the inner RadioGroup signal. emDirPanel / emDirEntryPanel `CreateControlPanel` synthesize a SchedCtx from the scheduler-reach PanelCtx to call the ctx-taking emFileManControlPanel::new. Helper fns in test_panel.rs (create_method_radio / create_dash_radio / create_stroke_end_radio) gain a `cc: &mut C` parameter; callers use a scratch TestSched so `ctx.tree` + ConstructCtx reach don't alias. A few set_behavior call sites hoist the widget ctor into a preceding `let __w = { ... };` block to avoid `ctx.tree` + `ctx.as_sched_ctx()` double-mutable-borrow. DIVERGED-B3.3 and DIVERGED-B3.4b markers remain in place pending B3.4c/d. Gate green — cargo fmt clean, clippy `-D warnings` clean, nextest 2438/0/31 (identical to B3.4a — pure signal allocation, no test pass/fail change), goldens 237/6 preserved, rc_refcell_total unchanged (grep-count ~246 including tests; no new Rc<RefCell>). No new unsafe, no new `#[allow]` apart from `too_many_arguments` on `make_factor_field` (whitelisted per CLAUDE.md). Pre-commit hook remains disabled until B3.4d keystone.

- **Task B3.4c (fire signals at Input paths; un-ignore Input-path tests; regression tests):** commit 234affbe. Per-widget Input-handler callback sites now fire the matching signal before invoking callbacks in C++ order (InvalidatePainting → Signal → callback for emCheckButton/emButton; Signal → InvalidatePainting for emSplitter per emSplitter.cpp:82-91; Signal(ColorSignal) post-sync for emColorField::Cycle per emColorField.cpp:205-207; per-event Signal(SelectionSignal)/Signal(FileTriggerSignal) inside emFileSelectionBox::Cycle at the emFileSelectionBox.cpp:124/190/309/401 sites; latched-drain for emTextField/emListBox so per-mutation Signal(...) cadence is preserved across one Input pass). **emCheckBox forced divergence (Option B):** Rust has no C++-style inheritance — emCheckBox gains its own `check_signal: SignalId` that mirrors C++ `emCheckButton::CheckSignal` which emCheckBox inherits publicly; Spec §3.5 D6.1. `new<C: ConstructCtx>(ctx, label, look)` migrated; all call sites updated (emAutoplayControlPanel, emStocksControlPanel, emStocksItemPanel, emCoreConfigPanel, emFileSelectionBox, goldens, pipeline/check — examples/ not in cargo target set and left unchanged). **Ctx-bearing setter variants added for Input-path drivers** (public setter `SetXxx` stays non-ctx, DIVERGED-B3.4d-refreshed, pending B3.4d migration): emScalarField `SetValueCtx`; emSplitter `SetPosCtx`; emRadioButton `SetCheckedCtx`/`SetCheckIndexCtx`; emDialog `FinishCtx`. emScalarField `Input/drag/StepByKeyboard` routes through `SetValueCtx`; emSplitter drag routes through `SetPosCtx`; emDialog Input Enter/Escape routes through `FinishCtx`. **emColorField::Cycle** grows a `ctx: &mut PanelCtx` parameter so the Cycle shim (behavior trait) can fire `color_signal` via `ctx.as_sched_ctx()`; all Cycle callers updated (behavior shim threads the existing ctx; unit tests build a scratch PanelCtx::new; behavioral/pipeline tests use `h.panel_ctx()`). **emTextField latch-drain:** three pending-fire flags (`pending_text_fire` / `pending_selection_fire` / `pending_can_undo_redo_fire`) set by `fire_change` / `fire_selection_change` / `fire_can_undo_redo`; `Input` renamed to delegate to `input_impl` and then call `drain_pending_fires(ctx)` which synthesizes a SchedCtx and fires each latched signal + its callback. `on_text` stays deferred (DIVERGED-B3.4d) — its `WidgetCallbackRef<str>` payload can't be forwarded from a latch because the text buffer mutates between mutations within an Input pass. **emListBox latch-drain with snapshots:** `pending_selection_snapshots: Vec<Vec<usize>>` records the selection at each `fire_selection` so drain invokes `on_selection` once per mutation (matching C++ SelectionChanged per-call cadence); `pending_item_trigger_fire` + `pending_trigger_index` for TriggerItem. Input delegates to `input_impl` + `drain_pending_fires`. `TriggerItem` remains non-ctx (DIVERGED-B3.4d). **Harness additions:** `EngineScheduler::clear_pending_for_tests()` (cfg(test)+test-support) drains pending signals without waking engines; `TestHarness` / `PipelineTestHarness` / all 13 per-widget `TestInit` structs gain a `Drop` impl that calls it so the scheduler's Drop debug_assert does not panic when tests fire signals without running a time slice. `TestHarness`/`PipelineTestHarness::panel_ctx()` helper returning a scheduler-reach `PanelCtx` rooted at the harness root. `with_panel_ctx_sched` generalised to return the closure's value. **12 Input-path tests un-ignored** (callback_receives_state, radio_group_callback, trigger_callback, selection_callback, callback_on_change, colorfield_cycle_fires_on_color_callback, splitter_on_position_callback_fires_during_drag, 6 × listbox pipeline triggers). **9 setter-path tests remain ignored** with refreshed B3.4d citations (set_value_fires_callback; RadioGroup remove_* callbacks; Dialog::Finish callback; TextField on_text + select + undo-redo direct-call; ListBox::TriggerItem direct-call; pipeline::radio bp13 programmatic SetCheckIndex). **12 new regression tests** asserting `scheduler.is_pending(widget.*_signal)` after Input for CheckButton, CheckBox, Button(Enter→click), RadioGroup(Enter→check), ScalarField(+-key→value), Splitter(drag→pos), ColorField(Cycle→color), Dialog(Enter→finish), TextField(insert→text + undo-redo), ListBox(arrow-down→selection + enter→item-trigger). Gate green — cargo fmt clean, clippy `-D warnings` clean, nextest 2463/0/18 (B3.4b was 2438/0/31 — net +25 passing / −13 skipped / +10 new regression / +2 net from test refactors), goldens 237/6 preserved. No new unsafe, no new `#[allow]`, no new Rc<RefCell<>>. Pre-commit hook remains disabled until B3.4d keystone.

- **Task B3.1 (PanelCtx fw_actions+root_context plumbing):** commit 59d5daf0. `PanelCtx` gains two new optional fields `framework_actions: Option<&'a mut Vec<DeferredAction>>` and `root_context: Option<&'a Rc<emContext>>` (matching the existing `scheduler`/`framework_clipboard` optionality pattern — unit tests that construct `PanelCtx::new` do not need to supply them). New combined builder `PanelCtx::with_sched_reach(tree, id, tallness, scheduler, framework_actions, root_context, framework_clipboard)` sets all four scheduler-reach handles at once — preferred over chaining `with_scheduler`+`with_clipboard` at production sites with full `EngineCtx` reach. New `PanelCtx::as_sched_ctx(&mut self) -> Option<SchedCtx<'_>>` synthesizes a full `SchedCtx` from the four handles, returning `None` if any is absent (no panic landmine — callers handle `None` explicitly). Construction-site migrations: `emPanelCycleEngine::Cycle` (aliased-borrow pattern extended to `framework_actions` with matching SAFETY comment), `emWindow::dispatch_input` per-panel PanelCtx, `tests/support/{mod,pipeline}.rs` input dispatch harnesses → all four now go through `with_sched_reach`. `emSubViewPanel::Input` threads through `ctx.framework_clipboard` (framework_actions / root_context not threaded — scheduler re-borrow aliasing constraints; sub-view Input continues to use its local `fw_input` buffer). `emFileManControlPanel` struct-literal construction extended with the two new `None` fields. Gate green — cargo check clean, clippy `-D warnings` clean, nextest 2459/0/9 (no change), goldens 237/6 (no regression). Pure plumbing: no widget / callback / signal changes.
