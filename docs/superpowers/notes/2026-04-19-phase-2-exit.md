# Phase 2 — View/Window Composition — Exit Baseline

**Captured:** 2026-04-20
**Branch:** port-rewrite/phase-2 @ 53944db0 (post-Task-10; pre-closeout commits)

## nextest

```
Summary [16.645s] 2458 tests run: 2458 passed, 9 skipped
```

## goldens

```
test result: FAILED. 237 passed; 6 failed; 0 ignored; 0 measured; 0 filtered out; finished in 29.16s
```

Same six baseline failures: `composition_tktest_{1,2}x`, `notice_window_resize`, `testpanel_{expanded,root}`, `widget_file_selection_box`.

## clippy

```
Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.16s
```

Zero warnings with `-D warnings`.

## fmt

`cargo fmt --check` clean.

## rc_refcell_total

262

## diverged_total

176

## rust_only_total (grep `RUST_ONLY:`)

17

## idiom_total

0

## try_borrow_total

0

## Delta from baseline

| Metric | Baseline | Exit | Δ |
|---|---|---|---|
| nextest passed | 2454 | 2458 | +4 |
| nextest failed | 0 | 0 | 0 |
| nextest skipped | 9 | 9 | 0 |
| goldens passed | 237 | 237 | 0 |
| goldens failed | 6 | 6 | 0 |
| clippy warnings | 0 | 0 | 0 |
| rc_refcell_total | 283 | 262 | −21 |
| diverged_total | 176 | 176 | 0 |
| rust_only_total (`RUST_ONLY:` comments) | 17 | 17 | 0 |
| rust_only filesystem markers (`.rust_only`) | — | — | +1 (`emPanelScope.rust_only` added) |
| idiom_total | 0 | 0 | 0 |
| try_borrow_total | 0 | 0 | 0 |

Interpretation:
- `rc_refcell_total −21`: Phase 2's raison d'être — `Rc<RefCell<emWindow>>` wholesale deletion plus `Rc<RefCell<emView>>` deletion from production. Handoff estimated −20 to −40; we landed −21, i.e., the conservative end because we left test-fixture `view_rc` holdouts (see below) for Phase 5.
- `diverged_total 0 net`: Task 10 removed 3 stale DIVERGED markers; offset by new ones added for genuinely forced divergences (e.g. `Box<emWindow>` for infinite-size avoidance, `PopupCloseSignal` borrow-avoidance mirror, NoticeList ring-node representation using `Option<PanelId>` vs C++'s pointer). Each new DIVERGED cites C++ by file:line.
- Tests added in Tasks 1, 2, 3, 9 (+4 matches the +4 delta).

## Invariant results (Phase-2-specific, C4)

- **I2** (`Rc<RefCell<emView>>` production, non-test): PASS — zero code hits; two comment/prose hits at `emView.rs:3215` and `emPanelTree.rs:3257` (the 3257 hit is a test-helper fn signature and is the test-fixture holdout, enumerated below under I2 test holdouts).
- **I2a** (`Weak<RefCell<emView>>` anywhere): PASS — four comment hits only, all DIVERGED-explanation prose in `emPanelCycleEngine.rs:33,55`, `emPanelTree.rs:226`, `composition.rs:70`. Zero code hits.
- **I2b** (`Rc<RefCell<emWindow>>` or `Weak<RefCell<emWindow>>`): PASS — two comment hits at `emGUIFramework.rs:94,553`. Zero code hits.
- **I2b-W3** (same as I2b's `Rc` clause): PASS — zero code hits; Task-W3 deferral closed by W3+4-backref (commit 6fdf5096).
- **I2c** (`view_rc|sub_view_rc` as identifiers): PASS **with test-fixture holdouts**:
  - `crates/emcore/src/emPanelTree.rs:3251-3273` — test helper `make_registered_tree` uses `Rc<RefCell<emView>>` via a `RcCell<T>` type alias (deliberate evasion of grep per comment, but the method signature at 3257 still binds `Rc<RefCell<emView>>`). Scheduled for Phase 5 removal.
  - `crates/emcore/src/emView.rs:7303-7368` — test `visiting_va_cycles_when_activated` binds `view_rc = Rc::new(RefCell::new(emView::new(...)))` as a local. Scheduled for Phase 5 removal.
  Per plan's I2 prose: "#[cfg(test)] helpers under `crates/eaglemode/tests/` may retain shape only where they model external C++-test fixtures — enumerate such holdouts in the closeout and schedule for removal by Phase 5." These two holdouts qualify.
- **I6 (partial)**: golden baseline 237/6 held.
- **NoticeList location check**: PASS — the ring heads `notice_ring_head_next`/`notice_ring_head_prev` live on `emView` (emView.rs:579,581); `PanelTree` holds only the `has_pending_notices` flag + `pending_ring_cleanup` reconciliation buffer, no ring structure.

## JSON entries closed

| Id | How resolved | Commit |
|---|---|---|
| E006 | NoticeList relocated to `emView` per spec §5 D5.5 (ring heads on emView, `HandleNotice` drain, `has_pending_notices` flag on PanelTree as the only remainder). DIVERGED block at old `emView.rs:3465` deleted; new storage DIVERGED annotates idiom-only divergence. | 68dbeadb (Task 6) |
| E014 | Engine back-refs migrated: `Weak<RefCell<emView>>` → `PanelScope` on `emPanelCycleEngine`, `UpdateEngineClass`, `VisitingVAEngineClass`. Views resolve via `PanelScope::resolve_view` or scope-aware dispatch. | 7549fc13 (Task 5) + cb5129e1 (keystone) |
| E015 | Focus consolidated onto `emView` per spec §5 D5.6: `emViewPort::focused` deleted; `SwapViewPorts` swaps `emView::window_focused` directly; port-level `SetViewFocused` / `RequestFocus` now doc stubs (no C++-API breakage for existing callers). | aea6f3db (Task 4 D5.6) |
| E038 | Borrow-ordering hazard dissolves with plain-value ownership: there is no longer a `Rc<RefCell<emView>>`/`Rc<RefCell<emWindow>>` to force a `.borrow()`-before-`.borrow_mut()` sequencing. Keystone green compile (no `RefCell` borrow panics anywhere in `cargo-nextest ntr`) is the verification. | cb5129e1 (keystone) |
