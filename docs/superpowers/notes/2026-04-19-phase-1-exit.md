# Phase 1 — Exit (captured at C2)

**Captured:** 2026-04-19 (closeout session)
**Branch:** `port-rewrite/phase-1`
**Status:** PARTIAL — Chunks 1+2 complete; Chunks 3+4 (keystone) deferred to Phase 1.5

## nextest

```
2455 tests run: 2455 passed, 9 skipped, 0 failed
```

## goldens

```
237 passed; 6 failed; 0 ignored; 0 measured; 0 filtered out
```

Baseline preserved (237/6 is the documented pre-Phase-1 baseline).

## clippy

Clean (no warnings emitted).

## rc_refcell_total

```
287
```

## diverged_total

```
177
```

## rust_only_total

```
16
```

## idiom_total

```
0
```

## try_borrow_total

```
11
```

## Delta

Baseline (from `2026-04-19-phase-1-baseline.md`) vs exit:

| metric              | baseline | exit | delta |
|---------------------|---------:|-----:|------:|
| nextest passed      |     2451 | 2455 |    +4 |
| nextest failed      |        0 |    0 |     0 |
| goldens passed      |      237 |  237 |     0 |
| goldens failed      |        6 |    6 |     0 |
| rc_refcell_total    |      284 |  287 |    +3 |
| diverged_total      |      177 |  177 |     0 |
| rust_only_total     |       16 |   16 |     0 |
| idiom_total         |        1 |    0 |    -1 |
| try_borrow_total    |       11 |   11 |     0 |

Notes:
- `nextest passed` +4 from new Chunk 1/2 unit tests (EngineCtx scaffolding exercisers, framework scheduler shape assertion).
- `rc_refcell_total` +3: Chunk 2's explicit re-wrap of `App.scheduler` as
  `Rc<RefCell<EngineScheduler>>` (DIVERGED at emGUIFramework.rs:96) plus
  related helper sites — carry-forward-phase-1.5. Net direction will go
  negative in Phase 1.5 once the keystone lands.
- `idiom_total` -1: IDIOM: comment block at emView.rs deleted (Tasks 4+5
  minimal @ d3a6643). Invariant I5 SAT.
- `try_borrow_total` unchanged: the 11 occurrences remain in
  `emView.rs` (4) + `emPanelTree.rs` (7). Phase 1.5 deletes them as
  part of keystone.
- `diverged_total` unchanged: Chunk 2 added one DIVERGED comment at
  `emGUIFramework.rs:89-95` (Ch2-A re-wrap) and Phase 1 minimal work
  did not remove SP4/SP4.5/SP8 DIVERGED blocks — those depend on the
  keystone and move in Phase 1.5.

## Invariants (C4 verification)

| ID   | Invariant                                                     | Status | Evidence / carry-forward |
|------|---------------------------------------------------------------|--------|--------------------------|
| I1   | `Rc<RefCell<EngineScheduler>>` zero matches in crates/        | UNSAT  | 4 matches (`emPanelTree.rs:3186`, `emGUIFramework.rs:96,649`, `emEngineCtx.rs:3` comment). CARRY-FORWARD to Phase 1.5. |
| I1a  | `SchedOp` zero matches                                        | UNSAT  | 10+ matches (emView, emPanelTree, emPanelCtx, emSubViewPanel). CARRY-FORWARD. |
| I1b  | `pending_sched_ops`/`queue_or_apply_sched_op`/`register_pending_engines`/`close_signal_pending` zero | UNSAT | Multiple sites. CARRY-FORWARD. |
| I1c  | `sub_scheduler` zero matches                                  | UNSAT  | `emSubViewPanel.rs:43,65,72` + comments. CARRY-FORWARD. |
| I1d  | no `try_borrow` in emView.rs / emPanelTree.rs                 | UNSAT  | 11 sites (emView.rs:201,654,2080,2087; emPanelTree.rs:397,400,403,550,573,577,590). CARRY-FORWARD. |
| I5   | no `IDIOM:` in crates/                                         | SAT    | `rg 'IDIOM:' crates/` empty. Closed by Tasks 4+5 @ d3a6643. |
| I6-a | no `NewRootWithScheduler`                                      | SAT    | `rg 'NewRootWithScheduler' crates/` empty. Closed by Chunk 2 @ 0e68a1f. |
| I6-b | no `fn GetScheduler`                                           | SAT    | `rg 'fn GetScheduler' crates/` empty. Closed by Chunk 2 @ 0e68a1f. |
| I6-c | no `emContext::scheduler` field                                | SAT    | Field deleted Chunk 2 @ 0e68a1f. |

## JSON entries closed (resolved-phase-1)

- **E001** — IDIOM comment block at `emView.rs:186–194`. Resolution: deleted
  @ `d3a6643` (phase-1 tasks 4+5 Option B minimal). Witness: `rg 'IDIOM:' crates/`
  returns empty; invariant I5 SAT.
- **E036** — Spec-doc reference to the E001 IDIOM block. Obsolete once E001
  resolved; no code change required. Marked resolved-phase-1 @ `d3a6643`.

## JSON entries carried forward to Phase 1.5

All of E002, E003, E004, E005, E007, E008, E009, E010, E011 remain UNSAT —
they all require the keystone migration (emView scheduler field deletion,
SchedOp enum deletion, ctx threading through emView methods, App.scheduler
re-narrowing, sub_scheduler deletion via PanelBehavior::Cycle signature
flip). See `docs/superpowers/notes/2026-04-19-phase-1-chunk-3-blocked.md`
and `2026-04-19-phase-1-chunk-4-blocked.md` for the structural analysis
that justifies deferral.
