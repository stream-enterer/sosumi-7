# Phase 1.5 — Exit (captured at C2)

**Captured:** 2026-04-20
**Branch:** `port-rewrite/phase-1-5`
**Status:** PARTIAL — Task 1 complete; Tasks 2–5 deferred

## nextest

```
2455 tests run: 2455 passed, 9 skipped, 0 failed
```

## goldens

```
237 passed; 6 failed; 0 ignored; 0 measured; 0 filtered out
```

## clippy

`cargo clippy --all-targets --all-features -- -D warnings`: PASS (clean).

## rc_refcell_total

```
282
```

## diverged_total

```
177
```

## rust_only_total

```
17
```

## idiom_total

```
0
```

## try_borrow_total

```
5
```

## Delta from Phase 1.5 baseline

| metric              | baseline | exit | delta |
|---------------------|---------:|-----:|------:|
| nextest passed      |     2455 | 2455 |     0 |
| nextest failed      |        0 |    0 |     0 |
| nextest skipped     |        9 |    9 |     0 |
| goldens passed      |      237 |  237 |     0 |
| goldens failed      |        6 |    6 |     0 |
| rc_refcell_total    |      287 |  282 |    −5 |
| diverged_total      |      177 |  177 |     0 |
| rust_only_total     |       16 |   17 |    +1 |
| idiom_total         |        0 |    0 |     0 |
| try_borrow_total    |       11 |    5 |    −6 |

### Interpretation

- **rc_refcell_total −5:** `emView::scheduler`, `App.scheduler` (Rc wrapper), `PanelTree::sched_rc`, plus two internal Rc holdings during attach. `emSubViewPanel::sub_scheduler` survives — Task 2 deferred.
- **try_borrow_total −6:** the entire `try_borrow_mut` deferral scaffolding around the `emView.scheduler`-reach-through pattern is gone. Remaining 5 hits: `emSubViewPanel::sub_scheduler` uses (4) plus 1 in emGUIFramework we haven't inspected.
- **rust_only_total +1:** `test_view_harness.rs` marker file (introduced by substep 1a) adds 1 RUST_ONLY comment.
- **goldens unchanged:** 237/6 baseline preserved — no pixel regressions.
- **nextest unchanged at 2455:** net-zero test count; scheduler-deletion path tests deleted, test-harness-consolidated tests retained.

## JSON entries closed this phase

Phase 1.5 resolves the substantial majority of the `emView`/`App` scheduler back-channel but does NOT close entries tied to `emSubViewPanel::sub_scheduler` (Task 2 territory) or `emPanelCtx.rs` deletion (Task 3 territory). Specific entries in flight for closure:

- (to be verified by C5 — JSON sweep)

## Phase-specific invariants

| ID | Description | Status |
|----|-------------|--------|
| I1 | outer scheduler threaded via ctx end-to-end | **PARTIAL** — outer done; sub_scheduler still separate |
| I1a | `SchedOp`/`queue_or_apply_sched_op` eliminated | **SAT** |
| I1b | `close_signal_pending`/`pending_sched_ops` eliminated | **SAT** |
| I1c | `sub_scheduler` eliminated | **UNSAT** — deferred |
| I1d | `emView::scheduler` field deleted | **SAT** |
| I6 | `NewRootWithScheduler`/`emContext::scheduler` gone | **SAT** (already from Phase 1) |

## Why PARTIAL

Task 2 attempted and blocked on a genuine structural issue — not a shim refusal.
`PanelCycleEngine` looks up panels via `ctx.tree`, so sub-tree panel engines
registered on the outer scheduler can't find their panels without a preceding
change to `PanelCycleEngine`'s tree-resolution. Neither Task 2 nor Task 3 as
currently drafted creates that change; it's a plan gap that deserves its own
brainstorm + plan revision, not a rushed in-phase fix. See the BLOCKED dispatch
in ledger and `crates/emcore/src/emSubViewPanel.rs` inline analysis.

Tasks 4 (popup-signal inline + timing-delta fix) and 5 (closeout prep) are
decoupled from Task 2 but were sequenced after it and inherit the pause.
