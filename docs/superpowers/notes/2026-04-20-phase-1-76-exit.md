# Phase 1.76 — Exit Metrics

**Captured:** 2026-04-20 local
**Branch:** `port-rewrite/phase-1-76` @ Task-4 closeout
**Baseline:** see `2026-04-20-phase-1-76-baseline.md`

## nextest

```
2454 tests run: 2454 passed, 9 skipped, 0 failed
```

## goldens

```
237 passed; 6 failed
```

Identical failure set as baseline (`composition_tktest_1x`, `composition_tktest_2x`,
`notice_window_resize`, `testpanel_expanded`, `testpanel_root`,
`widget_file_selection_box`). No Phase 1.76 regressions. Unmasked-wake
hypothesis did not surface a latent golden shift (wakes now reach the real
scheduler, but the six pre-existing failures are unrelated to Input dispatch).

## clippy

`cargo clippy --all-targets --all-features -- -D warnings`: PASS (clean).

## cargo fmt

`cargo fmt --check`: clean.

## rc_refcell_total

```
283
```

## diverged_total

```
176
```

## rust_only_total

```
17
```

## idiom_total

```
0
```

## try_borrow_total

```
0
```

## Delta

| metric              | baseline | exit | delta |
|---------------------|---------:|-----:|------:|
| nextest passed      |     2454 | 2454 |     0 |
| nextest failed      |        0 |    0 |     0 |
| nextest skipped     |        9 |    9 |     0 |
| goldens passed      |      237 |  237 |     0 |
| goldens failed      |        6 |    6 |     0 |
| rc_refcell_total    |      283 |  283 |     0 |
| diverged_total      |      176 |  176 |     0 |
| rust_only_total     |       17 |   17 |     0 |
| idiom_total         |        0 |    0 |     0 |
| try_borrow_total    |        0 |    0 |     0 |

All metrics held at baseline. Phase 1.76 was a purely mechanical cascade
(trait signature + call-site updates); no new Rc/RefCell, no new DIVERGED
blocks, no try_borrow sites, no idiom adaptations.

## Invariants verified

### Phase 1.76 new invariants

| ID | Status | Evidence |
|----|--------|----------|
| I-1.76-throwaway — no `throwaway_sched_input` or inline `EngineScheduler::new()` in production `emSubViewPanel.rs` | SAT | `rg 'throwaway_sched_input' crates/emcore/src/emSubViewPanel.rs` → 1 comment-only match (documenting removal; no binding); `rg 'EngineScheduler::new\(\)' crates/emcore/src/emSubViewPanel.rs` → only inside `#[cfg(test)]` at line 530. |
| I-1.76-signature — `PanelBehavior::Input` trait default has `_ctx: &mut PanelCtx` | SAT | `emPanel.rs:201–208` has the new parameter. |
| I-1.76-cascade — every production `.Input(...)` call site passes `&mut PanelCtx` | SAT | `cargo clippy --all-targets --all-features -D warnings` clean; all 19 production overrides have the new param. |
| I-1.76-no-new-hacks — `#[allow(...)]` count outside whitelist unchanged | SAT | 22 matches pre-phase and 22 matches post-phase; all are pre-existing. |

### Phase 1.75 carry-forwards

| ID | Status | Evidence |
|----|--------|----------|
| I1 — `rg 'Rc<RefCell<EngineScheduler>>' crates/` = 0 | SAT | 0 matches. |
| I1c — `rg -w 'sub_scheduler' crates/` = 0 | SAT | 0 matches. |
| I1d — `try_borrow(_mut)?()` in emView/emPanelTree/emSubViewPanel = 0 | SAT | 0 matches across all three files. |
| I-Y3-dispatch — EngineScheduler::DoTimeSlice walks TreeLocation per engine | SAT | `emScheduler.rs` unchanged by Phase 1.76. |
| I-T3a — `rg -w 'register_pending_engines' crates/` = 0 | SAT | 0 matches. |
| I-T3b — `emPanelCtx.rs` deleted; no `pub mod emPanelCtx` | SAT | file absent; grep confirms 0. |
| I-T3c — `rg 'emPanelCtx::' crates/` = 0 | SAT | 0 matches. |
| I-Spec-3.3-clarified — Spec §3.3 describes cross-tree dispatch | SAT | Task 7 Phase 1.75 (`236fddf1`); unchanged by Phase 1.76. |
| Task-10 — 4 inline `ctx.create_signal()` in emView::RawVisitAbs | SAT | `emView.rs` unchanged by Phase 1.76. |
| Task-11 — fixture asserts `delta == 0` | SAT | `phase_1_75_task6_spawn_and_wake_child_in_same_slice_delta_zero` present and passing. |

### Phase 1 carry-forwards

| ID | Status | Evidence |
|----|--------|----------|
| I1a — SchedOp eliminated | SAT | `rg 'SchedOp\|queue_or_apply_sched_op' crates/` = 0. |
| I1b — close_signal_pending / pending_sched_ops eliminated | SAT | grep = 0. |
| I6 — Golden tests 237/6 preserved | SAT | goldens 237/6 identical failure set. |

## JSON entries closed

Resolved in Phase 1.76:
- **E039** — `PanelBehavior::Input` throwaway `EngineScheduler::new()` at `emSubViewPanel.rs`. Resolved by KEYSTONE Task 2 commit (`48466f41`): throwaway deleted; trait signature widened to carry `ctx: &mut PanelCtx`; 19 production overrides + ~25 test overrides updated; `emWindow::dispatch_input` constructs `PanelCtx::with_scheduler` per panel.
